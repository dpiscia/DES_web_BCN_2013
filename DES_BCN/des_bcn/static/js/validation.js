// Generated by CoffeeScript 1.6.2
var appear_cf, disable_cf;

disable_cf = function(el_nbr, option) {
  var color;

  if (option) {
    color = "#eeeeee";
  } else {
    color = "#141414";
  }
  $("#item-deformField" + el_nbr).css("color", color);
  return $("#item-deformField" + el_nbr + ".control-group input:radio").attr('disabled', option);
};

appear_cf = function(child, parent, ids) {
  var el, _i, _len, _results;

  disable_cf(child, true);
  _results = [];
  for (_i = 0, _len = ids.length; _i < _len; _i++) {
    el = ids[_i];
    if (($("#item-deformField" + parent + ".control-group input:checked").val() === el) && (!($("#item-deformField" + parent + ".control-group input:radio").attr('disabled') === "disabled"))) {
      _results.push(disable_cf(child, false));
    } else {
      _results.push(void 0);
    }
  }
  return _results;
};
